# frozen_string_literal: true

RSpec.describe Segment::DreiQuarks::Rails do
  it 'has a version number' do
    expect(Segment::DreiQuarks::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
